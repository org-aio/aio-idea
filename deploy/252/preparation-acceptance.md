# 插件实例预备与编译缓存验收

日期：2026-09-14。目标站点：https://aio.addzero.site/。

## 发布版本

- 平台：`ffe76fb156c36aa34dd50a5a4f3894dc4c2c4f0a`，包含编译缓存、不可变资源元数据与统一生命周期桥。
- 共享壳：`a18dc3921768ab2d1854b8b42281d3db56485a9d`，包含 `a1c4f17` 的有界页面预备。
- 正式宿主：`745a326f0d13e50eb934376d71aa94e4b0a2cc7f`，252 和公网健康检查通过。首个候选 `fdb5465` 在上传阶段停止，没有激活。
- 旧壳页面跨发布保持打开，新宿主上线后直接挂载 KMP 并成功调用 Ktor，浏览器壳指纹未变化，未刷新页面。

## 行为

- 服务端验证包时计算前端资源摘要和大小，后续挂载复用元数据。
- Wasmtime 使用受宿主控制的持久目录缓存编译结果；相同引擎与配置复用机器码，配置变化重新编译。代码缓存不包含租户实例或授权。
- 登录后继续按摘要缓存资源，再按最近使用顺序准备桌面最多 2 页、手机最多 1 页；总量计入原有 6 页上限，不淘汰已访问页面来预热。
- iframe 隐藏且不可交互，提前实例化 Wasm；首次激活才允许业务请求并开始业务超时。等待队列限 16 项，租户暂停会拒绝等待请求。
- 后台预备使用入口参数 `__aio_prepare=1` 显式声明；普通可见挂载不等待预备握手，支持部署期间仍在运行的浏览器壳。
- 显示时保留同一 iframe 和 JavaScript 实例。浏览器可能暂停隐藏帧的绘制，所以首帧仍在实际显示后完成。
- 资源下载失败保留重试机制，单页准备超过 30 秒停止后续实例准备。省流量模式或低速网络跳过预热。首次下载、浏览器拒绝缓存或版本更新仍可能较慢。
- 发布前清理生成的 Web 输出目录，避免共享构建缓存中的历史脚本、Wasm 和样式被重复上传或预加载。

## 公网基线

旧宿主 `d759111b1abcb6120eae77490a0bd3fb1bda9e6c`，仅字节预热。测试经 252 出口代理访问正式 HTTPS，固定 HTTP/1.1；测量包含导航点击及 400 毫秒画布稳定等待，不代表用户其他网络的固定耗时。

| 视口 | 场景 | 后台资源预热 | 点击到 Counter 可操作 |
| --- | --- | --- | --- |
| 桌面 | 首次 | 33.621 秒 | 11.168 秒 |
| 桌面 | 刷新 | 3.620 秒 | 8.534 秒 |
| 手机 | 首次 | 33.791 秒 | 12.594 秒 |
| 手机 | 刷新 | 3.557 秒 | 6.707 秒 |

四次均无重复 Wasm 下载，后台业务调用为零，打开时各新建一次挂载；真实 Ktor 请求、Counter 计数及切换保留通过。

## 公网结果

在同一测试网络、同一插件版本下运行新宿主，2026-09-14 09:40:33 CST 激活，服务 PID 14255。

| 视口 | 场景 | 后台准备 | 点击到 Counter 可操作 | 之前 |
| --- | --- | --- | --- | --- |
| 桌面 | 首次 | 22.894 秒 | 1.762 秒 | 11.168 秒 |
| 桌面 | 刷新 | 6.418 秒 | 1.728 秒 | 8.534 秒 |
| 手机 | 首次 | 24.289 秒 | 1.548 秒 | 12.594 秒 |
| 手机 | 刷新 | 5.561 秒 | 1.717 秒 | 6.707 秒 |

四次均复用原 iframe，打开时零新挂载、零 KMP Wasm 重复下载；准备阶段零业务请求。真实 Ktor `/tasks` 调用成功，`KMP Counter1` 加一后画布像素发生变化，切换回来计数仍为 1。桌面及手机没有水平溢出。返回操作为 0.157–0.700 秒。

首次准备仍包含下载，刷新后仍需重新实例化浏览器 UI；测量开始于后台准备完成之后，不代表首次访问立即点击也能在 2 秒内打开。测试使用 Chrome 与出口代理、包含固定的画布等待，是本次 E2E 的观测值，不是所有设备或网络的性能保证。

服务端代码缓存已落盘：`/opt/aio-idea/compiled` 为 360 KiB、0700；`/opt/aio-idea/component-storage/compiled` 为 2.8 MiB、0755，均归 `aio-shell` 所有且其他用户不能写入。引擎重建命中与编译配置变更失效由真实 Wasmtime 单元测试验证；本次没有为测速再次重启生产服务。

发布产物清理后只包含一个带摘要的壳 JavaScript 入口和一个 Wasm，样式预加载从残留目录中的 35 项降至当前 22 项。

## 本地验证

使用构建后的真实壳与 Compose Wasm；仅 API 为隔离协议夹具。

| 视口 | 场景 | 后台准备 | 点击到 Counter 可操作 |
| --- | --- | --- | --- |
| 桌面 1440 x 1000 | 首次 | 3.940 秒 | 1.823 秒 |
| 桌面 1440 x 1000 | 刷新 | 3.019 秒 | 1.953 秒 |
| 手机 390 x 844 | 首次 | 3.707 秒 | 1.541 秒 |
| 手机 390 x 844 | 刷新 | 2.776 秒 | 1.463 秒 |

操作时间包含测试主动等待画布稳定的 400 毫秒。首次准备后额外隐藏 37 秒，确认正常业务超时窗口之后仍无业务调用；显示后零重新挂载。画布点击计数、切换后状态保留、页面无水平溢出均通过。

相同 Compose 产物通过 v2 宿主和二进制桥重复执行，四次操作耗时为 1.720、1.576、1.602、1.493 秒；后台零业务、零重新挂载和画布状态检查均通过。

- 宿主 Rust：56 通过，9 个需要独立数据库或产物的既有用例未运行。
- 平台：包验证 12 通过；运行时 10 通过、5 个既有数据库用例未运行；浏览器 SDK 11 通过。
- 共享壳：16 通过，覆盖预备容量、激活、版本、租户和 LRU。
- 宿主资源、预热及通信桥：23 通过。
- `keepalive.cjs` 桌面/手机：租户 A/B/A、票据撤销、刷新缓存、版本替换、LRU、权限和会话变化通过。
- Compose 夹具没有打包中文回退字体，已有的 Google 字体请求被 CSP 拒绝。测试单独记录这类拒绝，其他控制台错误仍导致失败；本次未放宽 CSP。

机器生成的 JSON、截图与日志位于 `target/preparation-test/` 和 `target/keepalive-test/`。公网测试使用测试会话，只读调用真实 Ktor `/tasks`，计数仅修改当前浏览器内存。

公网证据：`target/preparation-test/public/{baseline,prepared,rolling}.json`；桌面/手机截图为同目录下的 `prepared-*.png`。v2 协议夹具证据在 `target/preparation-test/component/`。测试修正了滚动场景、iframe 初始化脚本作用域和按目标插件统计下载的计数方式，没有通过修改正式插件、发布接口或测试激活接口绕过加载过程。
